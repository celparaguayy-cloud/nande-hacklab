import type { Curso } from "../courseTypes";

/**
 * Cursos del módulo "linux". Se completa con cursos interactivos completos
 * (concept + quiz + build + lab) ligados a las herramientas reales de ÑANDE.
 */
export const LINUX_COURSES: Curso[] = [];
